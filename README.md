# Chat App Using React.js Socket.io

go to root directory and run `npm install`
run the front end using `npm start` command
go to backend directory and run `npm install`
run the backend using `node app.js` command
 
